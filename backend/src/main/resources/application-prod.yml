spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: false

app:
  auth:
    cookie:
      secure: true
      same-site: Lax
      domain: .issuetok.site